name: My CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Code checkout
        uses: actions/checkout@v3

      - name: Show files
        run: ls

      - name: Run simple command
        run: echo "CI is working 🚀"